const customer = {
  name1: 'hari',
  place: 'bangalore',
  state: 'ka'
};

let name2 = customer.name1;
let place = customer.place;

// let { name1: nm, place } = customer;

console.log(name2);
console.log(place);
